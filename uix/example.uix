App {
  Title(text: "User List")

  for (user in users) {
    Row {
      Text(text: user.name)
    }
  }

  Input(bind: name)

Button(text: "Greet", onClick: greet)

  if (showMore) {
    Button(text: "Show More", onClick: toggle)
  }
}